import 'package:flutter/material.dart';

class StartOfTable extends StatefulWidget {
  @override
  _StartOfTableState createState() => _StartOfTableState();
}

class _StartOfTableState extends State<StartOfTable> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('temp'),
      ),
      body: Text('temp'),
    );
  }
}
